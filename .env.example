# Database
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Pusher (for multiplayer racing)
# Get these from https://pusher.com/
PUSHER_APP_ID=""
PUSHER_KEY=""
PUSHER_SECRET=""
PUSHER_CLUSTER=""

# Client-side Pusher (must be prefixed with VITE_ to expose to browser)
VITE_PUSHER_KEY=""
VITE_PUSHER_CLUSTER=""
